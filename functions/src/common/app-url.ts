/**
 * URL pública de pulse-app. La usan los flujos OAuth que terminan en un
 * redirect del navegador de vuelta a la app (instalación de GitHub, conexión
 * de una org de Salesforce), que son los únicos lugares del backend que
 * necesitan saber dónde vive el frontend.
 */
export const PULSE_APP_URL = 'https://pulse-app--pulse-app-93.us-east4.hosted.app';
